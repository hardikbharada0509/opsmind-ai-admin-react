export const APP_CONFIG = {
  // Mock API base URL for network setup, though there's no real backend.
  API_BASE_URL: import.meta.env.VITE_API_BASE_URL || "https://api.mockadmin.local",
  APP_NAME: "OpsMind AI Admin",
};
